# Example environment variables for Neuralight Pro
# Client-side configuration
NEXT_PUBLIC_MEMBERSTACK_PUBLIC_KEY=pk_live_example
NEXT_PUBLIC_MEMBERSTACK_APP_ID=app_example
NEXT_PUBLIC_XANO_API_URL=https://xano.io/api:example
NEXT_PUBLIC_XANO_API=https://xano.io/api:legacy-example

# Server-side secrets (never commit real values)
OPENAI_API_KEY=sk-your-openai-key
MEMBERSTACK_SECRET_KEY=msk_your_backend_secret
MEMBERSTACK_VERIFY_ENDPOINT=https://api.memberstack.com/v2/members/verify-access-token
API_ACCESS_TOKEN=super-secure-random-token
GCS_BUCKET_NAME=your-photo-bio-assets
GCP_PROJECT_ID=your-gcp-project

# Only required when mounting a JSON key instead of Workload Identity
GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/google/key.json
# Optional: provide the raw JSON if your platform expects inline credentials
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account",...}

# Optional overrides for local development
NODE_ENV=development
